server:
  port: 8888


spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Pransquare/CloudConfig.git
          clone-on-start: true

eureka:
  client:
    service-url:
      defaultZone: http://16.171.44.212:8761/eureka/
  instance:
    prefer-ip-address: true
