server:
  port: 8888
  address: 0.0.0.0   # Bind to all network interfaces, including the EC2 IPv4

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Pransquare/CloudConfig.git
          clone-on-start: true
